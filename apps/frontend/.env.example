# Public API base (browser + server)
NEXT_PUBLIC_BASE_URL="http://localhost:3009"

# Same value as API `STOREFRONT_API_KEY` for `/api/categories-public` and `/api/packages-public*`
NEXT_PUBLIC_STOREFRONT_API_KEY="generate-a-long-random-secret"
